---
export interface Props {
	date: Date
}

const { date } = Astro.props
---

<time class='font-r text-lg font-bold text-opacity-60' datetime={date.toISOString()}>
	{
		date.toLocaleDateString('en-us', {
			year: 'numeric',
			month: 'short',
			day: 'numeric'
		})
	}
</time>
